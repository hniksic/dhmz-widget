# HAProxy CORS proxy for DHMZ, pljusak.com, and yr.no weather data
# Accepts requests in standard CORS proxy format:
#   http://<host>:8080/https://vrijeme.hr/hrvatska1_n.xml
#   http://<host>:8080/https://pljusak.com/karta.php
#   http://<host>:8080/https://www.yr.no/api/v0/locations/...

listen weather_proxy
    bind *:8080
    mode http

    acl is_pljusak path_beg /https://pljusak.com/
    acl is_dhmz path_beg /https://vrijeme.hr/
    acl is_yrno path_beg /https://www.yr.no/

    # Security: only allow known origins (and OPTIONS preflight)
    http-request deny unless is_pljusak || is_dhmz || is_yrno || METH_OPTIONS
    http-request return status 204 if METH_OPTIONS

    # Set Host header and capture backend choice before path rewrite
    # (use-server evaluates ACLs after rewrite, so we save the choice
    # in a variable while the original path is still available)
    http-request set-header Host pljusak.com if is_pljusak
    http-request set-header Host vrijeme.hr if is_dhmz
    http-request set-header Host www.yr.no if is_yrno
    http-request set-var(txn.backend) str(pljusak) if is_pljusak
    http-request set-var(txn.backend) str(dhmz) if is_dhmz
    http-request set-var(txn.backend) str(yrno) if is_yrno

    # Strip the origin prefix, leaving just the path for the upstream
    http-request set-path %[path,regsub(^/https://pljusak\.com,,)] if is_pljusak
    http-request set-path %[path,regsub(^/https://vrijeme\.hr,,)] if is_dhmz
    http-request set-path %[path,regsub(^/https://www\.yr\.no,,)] if is_yrno

    # CORS headers
    http-response set-header Access-Control-Allow-Origin "*"
    http-response set-header Access-Control-Allow-Methods "GET, OPTIONS"
    http-response set-header Access-Control-Allow-Headers "Content-Type"

    # Server selection (using saved variable, not path-based ACLs)
    use-server pljusak if { var(txn.backend) -m str pljusak }
    use-server dhmz if { var(txn.backend) -m str dhmz }
    use-server yrno if { var(txn.backend) -m str yrno }
    server dhmz vrijeme.hr:443 ssl ca-file /etc/ssl/certs/ca-certificates.crt sni str(vrijeme.hr)
    server pljusak pljusak.com:443 ssl ca-file /etc/ssl/certs/ca-certificates.crt sni str(pljusak.com)
    server yrno www.yr.no:443 ssl ca-file /etc/ssl/certs/ca-certificates.crt sni str(www.yr.no)
